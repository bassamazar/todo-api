{
  "name": "todo-api",
  "version": "1.0.0",
  "description": "Todo API project",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "migrate": "npx prisma migrate dev --name init"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "express": "^4.21.0",
    "joi": "^18.2.2"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  }
}
